package homework_09;

public class Task2 {

 /*   // Метод для печати массива в обратном порядке
    public static void printArrayReversed(int[] array) {
        System.out.print("Массив в обратном порядке: [");
        for (int i = array.length - 1; i >= 0; i--) {
            System.out.print(array[i]);
            if (i != 0) {
                System.out.print(", ");
            }
        }
        System.out.println("]");
    }

    // Перегруженный метод: вывод массива с разделением по индексу
    public static void printArrayWithIndex(int[] array, int index) {
        System.out.print("Массив с индексом " + index + ": [");

        // Печать левой части в прямом порядке
        for (int i = 0; i < index; i++) {
            System.out.print(array[i] + ", ");
        }

        // Печать правой части в обратном порядке
        for (int i = array.length - 1; i >= index; i--) {
            System.out.print(array[i]);
            if (i != index) {
                System.out.print(", ");
            }
        }
        System.out.println("]");
    }

    // Перегруженный метод: массив и булевый флаг
    public static void printArrayWithFlag(int[] array, boolean reverse) {
        if (reverse) {
            System.out.print("Массив в обратном порядке: [");
            for (int i = array.length - 1; i >= 0; i--) {
                System.out.print(array[i]);
                if (i != 0) {
                    System.out.print(", ");
                }
            }
        } else {
            System.out.print("Массив в прямом порядке: [");
            for (int i = 0; i < array.length; i++) {
                System.out.print(array[i]);
                if (i != array.length - 1) {
                    System.out.print(", ");
                }
            }
        }
        System.out.println("]");
    }

    public static void main(String[] args) {
        int[] array = {1, 4, 3, 6, 7};

        // Вызов метода для обратного порядка
        printArrayReversed(array);  // Выведет [7, 6, 3, 4, 1]

        // Вызов перегруженного метода с индексом
        printArrayWithIndex(array, 2);  // Выведет [1, 4, 7, 6, 3]

        // Вызов перегруженного метода с флагом
        printArrayWithFlag(array, true);  // Выведет [7, 6, 3, 4, 1]
        printArrayWithFlag(array, false); // Выведет [1, 4, 3, 6, 7]
    }*/

}
