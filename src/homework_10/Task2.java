package homework_10;
//Дмитрий Пушкарь; группа 52.1; 23.09.2024
public class Task2 {
    /*
    public static String[] MinMax(String[] array) {
        if (array == null || array.length == 0) {
            return new String[0];
        }
        String shortest = array[0];
        String longest = array[0];

        for (String str : array) { // Цикл for-each. Перебираем каждый элемент массива array и присваиваем его переменной str.
            if (str.length() < shortest.length()) {
                shortest = str;
            }
            if (str.length() > longest.length()) {
                longest = str;
            }
        }
        return new String[]{shortest, longest};
    }
    public static void main(String[] args) {
        String[] strings = {"123456", "12345", "1234", "123", "1234567"};
        String[] result = MinMax(strings);
        System.out.println("Самая короткая строка: " + result[0]);
        System.out.println("Самая длинная строка: " + result[1]);
    }
    */
}
